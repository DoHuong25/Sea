<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đăng nhập</title>
    <style>
        body { 
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
            background: linear-gradient(135deg, #f5f7fa, #c3cfe2);
            display: flex; 
            justify-content: center; 
            align-items: center; 
            min-height: 100vh; 
            margin: 0; 
        }
        .form-container { 
            background-color: #fff; 
            padding: 40px; 
            border-radius: 12px; 
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1); 
            width: 100%; 
            max-width: 400px; 
            text-align: center; 
        }
        h2 { 
            color: #333; 
            margin-bottom: 30px; 
            font-size: 2em; 
            font-weight: 600; 
        }
        label { 
            display: block; 
            text-align: left; 
            margin-bottom: 8px; 
            color: #555; 
            font-weight: bold; 
        }
        input[type="text"], input[type="password"] { 
            width: 100%; 
            padding: 12px; 
            margin-bottom: 25px; 
            border: 1px solid #ddd; 
            border-radius: 8px; 
            box-sizing: border-box; 
            font-size: 16px; 
            transition: border-color 0.3s, box-shadow 0.3s; 
        }
        input[type="text"]:focus, input[type="password"]:focus { 
            border-color: #e44d26; 
            box-shadow: 0 0 0 3px rgba(228, 77, 38, 0.1); 
            outline: none; 
        }
        button { 
            padding: 12px 20px; 
            border: none; 
            border-radius: 8px; 
            cursor: pointer; 
            font-size: 18px; 
            width: 100%; 
            transition: all 0.3s ease; 
            font-weight: 600; 
        }
        .submit-btn { 
            background: #e44d26; 
            color: white; 
        }
        .submit-btn:hover { 
            background: #c0392b;
        }
        .register-btn { 
            margin-top: 15px; 
            background: #f0f0f0; 
            color: #333; 
        }
        .register-btn:hover { 
            background: #e0e0e0; 
        }
        .error-message { 
            color: #c0392b; 
            background-color: #fdd; 
            padding: 10px; 
            border-radius: 8px; 
            margin-bottom: 20px; 
            border: 1px solid #fbb; 
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h2>Đăng Nhập</h2>
        <% if (locals.error) { %>
            <p class="error-message"><%= error %></p>
        <% } %>
        <form method="POST" action="/login">
            <label for="username">Tên đăng nhập:</label>
            <input type="text" id="username" name="username" placeholder="Nhập tên đăng nhập" required>

            <label for="password">Mật khẩu:</label>
            <input type="password" id="password" name="password" placeholder="Nhập mật khẩu" required>

            <button type="submit" class="submit-btn">Đăng nhập</button>
        </form>
        <button type="button" class="register-btn" onclick="window.location.href='/register';">Chưa có tài khoản? Đăng ký</button>
    </div>
</body>
</html>
